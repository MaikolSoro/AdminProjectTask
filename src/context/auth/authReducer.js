
// import {
// 	SUCCESSFUL_REGISTRATION,
// 	REGISTRATION_ERROR,
// 	GETTING_USER,
// 	LOGIN_SUCCESSFUL,
// 	LOGIN_ERROR,
// 	LOGOUT
// } from '../../types';

export default (state, action) => {
	switch (action.type) {


		default:
			return state;
	}
}